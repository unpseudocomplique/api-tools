{
  "name": "api-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "prisma generate && nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "start": "npx prisma migrate deploy && nuxi build && nuxi start",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxt/ui": "^2.14.2",
    "@nuxt/ui-pro": "^1.4.4",
    "@prisma/client": "^5.11.0",
    "bcrypt": "^5.1.1",
    "fluent-ffmpeg": "^2.1.3",
    "generate-api-key": "^1.0.2",
    "nuxt": "^3.13.0",
    "nuxt-auth-utils": "^0.0.16",
    "pdf-lib": "^1.17.1",
    "sharp": "^0.33.2",
    "stripe": "^14.21.0"
  },
  "devDependencies": {
    "prisma": "^5.11.0"
  }
}
